<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipe Sharing</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Additional CSS specific to this page can go here */
        

       

        

        
        .recipe-grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    gap: 20px;
}

.recipe-card {
    flex: 0 1 calc(33.33% - 20px); /* Adjust based on number of cards per row */
    background-color: #fff;
    padding: 20px;
    border: 1px solid #ccc;
    border-radius: 5px;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    display: flex;
    flex-direction: column;
}

.recipe-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
}

.recipe-card img {
    max-width: 100%;
    height: auto;
    display: block;
    border-radius: 5px;
    margin-bottom: 10px;
    object-fit: cover; /* Ensures images maintain aspect ratio and fill container */
    flex-grow: 1; /* Ensures the image takes up remaining space */
}

.recipe-card h2 {
    font-size: 1.5rem;
    margin-bottom: 10px;
    color: #333;
}

.recipe-card p {
    font-size: 1rem;
    color: #666;
    margin-bottom: 10px;
}

/* Ensures uniform height for all recipe cards */
.recipe-card > div {
    display: flex;
    flex-direction: column;
    height: 100%;
}
    </style>
</head>
<body>
    <div class="container">
        <h1 class="animated-heading">
            <% for (let i = 0; i < 'CHAPPAN BHOG'.length; i++) { %>
                <span><%= 'CHAPPAN BHOG'[i] %></span>
            <% } %>
        </h1>
        <form class="search-form" action="/recipes" method="GET">
            <input type="text" name="search" placeholder="Search for recipes...">
            <button type="submit">Search</button>
        </form>
        <button class="add-recipe-button" onclick="location.href='/recipes/add'">Add Recipe</button>
        <div class="recipe-grid">
            <% recipes.forEach(recipe => { %>
                <a class="recipe-card" href="/recipes/<%= recipe._id %>">
                    <img src="<%= recipe.image %>" alt="<%= recipe.title %>">
                    <h2><%= recipe.title %></h2>
                    <p><%= recipe.description %></p>
                </a>
            <% }); %>
        </div>
    </div>
</body>
</html>
